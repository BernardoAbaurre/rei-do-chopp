using System;
using {{namespace}}.Domain.Utils.Enums;
using {{namespace}}.Domain.Utils.Filters;

namespace {{namespace}}.Domain.{{pluralName}}.Repositories.Filters
{
    public class {{pluralName}}ListFilter : PaginationFilter
    {
        public int? Id { get; set; }
{{#each properties}}
        {{#if (isObject propertyType)}}
        public int? {{propertyName}}Id { get; set; }
        {{else}}
        public {{propertyType}}{{#if (isNullable propertyType)}}?{{else}}{{#ifEquals propertyType "string"}}{{else}}?{{/ifEquals}}{{/if}} {{propertyName}} { get; set; }
        {{/if}}
{{/each}}


        public {{pluralName}}ListFilter() : base(pageSize: 10, ordenationField: "Id", ordenationType: OrdenationTypeEnum.ASC) {}
    }
}