using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc;
using {{namespace}}.Application.{{pluralName}}.Services.Interfaces;
using {{namespace}}.DataTransfer.{{pluralName}}.Request;
using {{namespace}}.DataTransfer.{{pluralName}}.Response;
using {{namespace}}.DataTransfer.Pagination.Responses;

namespace {{namespace}}.API.Controllers.{{pluralName}}
{
    [ApiController]
    [Authorize]
    [Route("api/{{camelCase pluralName}}")]
    public class {{pluralName}}Controller : ControllerBase
    {
        private readonly I{{pluralName}}AppService {{camelCase pluralName}}AppService;

        public {{pluralName}}Controller(I{{pluralName}}AppService {{camelCase pluralName}}AppService)
        {
            this.{{camelCase pluralName}}AppService = {{camelCase pluralName}}AppService;
        }

        [HttpGet("{id}")]
        public async Task<ActionResult<{{name}}Response>> GetAsync(int id)
        {
            var response = await {{camelCase pluralName}}AppService.ValidateAsync(id);

            return Ok(response);
        }

        [HttpGet]
        public async Task<ActionResult<PaginationResponse<{{name}}Response>>> ListAsync([FromQuery] {{name}}ListRequest request)
        {
            var response = await {{camelCase pluralName}}AppService.ListAsync(request);
            return Ok(response);
        }

        [HttpPost]
        public async Task<ActionResult<{{name}}Response>> InsertAsync([FromBody] {{name}}InsertRequest request)
        {
            var response = await {{camelCase pluralName}}AppService.InsertAsync(request);
            return Ok(response);
        }

        [HttpPut("{id}")]
        public async Task<ActionResult<{{name}}Response>> EditAsync(int id, [FromBody] {{name}}EditRequest request)
        {
            var response = await {{camelCase pluralName}}AppService.EditAsync(id, request);
            return Ok(response);
        }
    }
}
